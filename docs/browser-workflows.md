# Browser workflows

Superseded. The Superføring DOM contract, button semantics and driver now live in the skill:

- `skills/fiken-superforing/reference/superforing-dom.md` — URL, row model, buttons, primitives
- `skills/fiken-superforing/surfaces/cdp.md` — `scripts/superforing.mjs`, the CDP driver
- `skills/fiken-superforing/flows/` — one file per line kind

The original exploration notes are kept at `docs/archive/browser-workflows-2026-09.md` for the
worked run and the reasoning behind the render-race and time-budget rules.
